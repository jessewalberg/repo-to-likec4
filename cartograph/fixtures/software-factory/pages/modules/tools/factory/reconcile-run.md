---
node: module:tools/factory/reconcile-run.mjs
provenance: machine
pinned: false
---
# reconcile-run.mjs

Executes or coordinates reconciliation workflows by importing and leveraging the `reconcile.mjs` module. As an endpoint with no inbound dependents, it likely serves as a standalone runner—possibly a Node CLI script or factory tool invoked directly from the build/test system rather than as a utility imported by other components.
