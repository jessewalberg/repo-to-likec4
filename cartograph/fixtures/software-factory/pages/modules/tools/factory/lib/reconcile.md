---
node: module:tools/factory/lib/reconcile.mjs
provenance: machine
pinned: false
---
# reconcile.mjs

Standalone library providing reconciliation functionality used by the reconcile-run orchestrator. As a self-contained module with zero imports, it encapsulates the core logic in the factory's tools without external dependencies. Imported by reconcile-run.mjs, which likely handles execution and coordination of this reconciliation implementation.
